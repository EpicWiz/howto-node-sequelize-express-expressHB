<div class="container text-center" style="padding:10px 0; margin:0 0 30px 0; width: 100%; background-color: #F5F5F5;">EXAMPLE</div>
<div class="container">
  <div class="row">
    <div class="col-sm-4 text-center">
      <h3>Page Route</h3><br>
      <a href="/anotherPage"><button class="btn btn-default btn-lg">Another Page</button></a>
    </div>
    <div class="col-sm-4 panel">
      <h3>Add or Update DB Table Data</h3>
      <h4 style="color:red;">Add</h4>
      <!-- YOU MUST SET A NAME ATTRIBUTE IF YOU WANT THE DATA TO BE ACCESSIBLE ON request.body -->
      <form action="/api/addNew" method="POST" class="form-inline text-left panel-body">
        <div class="form-group">
          <label for="name">Name:</label><br>
          <input type="text" class="form-control" name="name" id="name">
        </div>
        <div class="form-group">
          <label for="animal">Favorite Animal:</label><br>
          <input type="text" class="form-control" name="animal" id="animal">
        </div>
        <br><br><button type="submit" class="btn btn-default">Add</button>
      </form>

      <h4 style="color:red;">Update</h4>
      <!-- USE ?_method=PUT IN THE ACTION ATTRIBUTE AFTER YOUR NORMAL ROUTE TO UPDATE TABLE DATA -->
      <form action="/api/change?_method=PUT" method="POST" class="form-inline text-left panel-body">
        <div class="form-group">
          <label for="name">Name:</label><br>
          <input type="text" class="form-control" name="name2" id="name2">
        </div>
        <div class="form-group">
          <label for="animal">Favorite Animal:</label><br>
          <input type="text" class="form-control" name="animal2" id="animal2">
        </div><br>
        <div class="form-group">
          <label for="updateId"></label><br>
          <select class="state form-control" name="updateId" id="updateId">
            <option>Select One</option>
            {{#each animal}}
              <option value="{{this.id}}">{{this.name}} ({{this.animal}})</option>
            {{/each}}
          </select>
        </div>
        <br><br><button type="submit" class="btn btn-default">Update</button>
      </form>
    </div>
    <div class="col-sm-4 text-center">
      <h3>Delete Data</h3>
      <!-- TO DELETE AND UPDATE TABLE DATA, YOU MUST USE A FORM WITH method="POST". <a> ELEMENTS ARE GET REQUESTS BY DEFAULT AND WILL NOT WORK FOR EITHER PUT OR DELETE-->
      <!-- I realize the grammar is wrong here for the animal or the animal's food (depending on how you entered it).
      If it bothers you, try writing a script to check for and add/remove 's' from the animal as needed-->
      {{#each animal}}
            <form class="delForm" action="/api/delete/{{this.id}}?_method=DELETE" method="POST">
              <div class="form-group">
                <label for="pHolder">{{this.name}} likes {{this.animal}}. {{this.animal}} eats {{this.Food.food}}!</label>
                <input type="text" style="display:none;" name="pHolder" id="pHolder">
                <button class="btn btn-default btn-sm" type="submit">X</button>
              </div>
            </form>
      {{/each}}
    </div>
  </div>
</div>

<div class="container-fluid" style="padding: 0; margin: 0;">
  <div class="row" style="padding:0; margin:0; width: 100%; background-color: #F5F5F5;">
    <div class="col-sm-12 text-center" style="padding: 5px 0; background-color: #F5F5F5;">
      <a href="https://github.com/EpicWiz/howto-node-sequelize-express-expressHB">
        <button class="btn btn-default btn-sm">Visit Repo</button>
      </a>
    </div>
  </div>
</div>
